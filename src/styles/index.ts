import React from 'react';

export const defaultScrollContainerStyles: React.CSSProperties = {
  display: 'flex',
  height: 'max-content',
  overflowY: 'hidden',
  position: 'relative',
  width: '100%',
};

export const defaultWrapperStyles: React.CSSProperties = {
  width: '100%',
  display: 'flex',
};

export const defaultLeftArrowStyle: React.CSSProperties = {
  transition: 'opactiy 0.3s',
  opacity: 1,
};

export const defaultRightArrowStyle: React.CSSProperties = {
  transition: 'opactiy 0.3s',
  opacity: 1,
};
